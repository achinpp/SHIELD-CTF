# S.H.I.E.L.D. // crawler policy
#
# Nothing on this host is meant to be indexed. The operational areas are
# listed explicitly rather than behind a blanket rule so the policy stays
# readable to whoever inherits it.

User-agent: *
Disallow: /challenges/
Disallow: /archive/incident.html
Disallow: /archive/internal/traces/
